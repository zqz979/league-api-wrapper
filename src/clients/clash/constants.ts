const BASE = '/lol/clash';
const VERSION = 'v1';
const TEAMS = `${BASE}/${VERSION}/teams`;
const TOURNAMENTS = `${BASE}/${VERSION}/tournaments`;
const TOURNAMENTS_BY_TEAM_ID = `${TOURNAMENTS}/by-team`;
const PLAYERS_BY_PUUID = `${BASE}/${VERSION}/players/by-puuid`;

export {TEAMS, TOURNAMENTS, TOURNAMENTS_BY_TEAM_ID, PLAYERS_BY_PUUID};
