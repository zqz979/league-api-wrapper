const BASE = '/riot/account';
const VERSION = 'v1';
const ACCOUNTS = `${BASE}/${VERSION}/accounts`;
const BY_RIOT_ID = `${ACCOUNTS}/by-riot-id`;
const BY_PUUID = `${ACCOUNTS}/by-puuid`;
const ACTIVE_REGION = `${BASE}/${VERSION}/active-region`;
export {BY_RIOT_ID, BY_PUUID, ACTIVE_REGION};
