const BASE = '/lol/spectator'
const VERSION = 'v5'

const ACTIVE_GAMES_BY_SUMMONER = `${BASE}/${VERSION}/active-games/by-summoner`

const FEATURED_GAMES = `${BASE}/${VERSION}/featured-games`

export { ACTIVE_GAMES_BY_SUMMONER, FEATURED_GAMES }
