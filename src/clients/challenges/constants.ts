const BASE = '/lol/challenges';
const VERSION = 'v1';
const CHALLENGES = `${BASE}/${VERSION}/challenges`;
const PERCENTILES = `${CHALLENGES}/percentiles`;
const PLAYER_DATA = `${BASE}/${VERSION}/player-data`;
const CONFIG = `${CHALLENGES}/config`;
export {CHALLENGES, PERCENTILES, PLAYER_DATA, CONFIG};
